**Badge** — small pill label for micro-status and category tags.

```jsx
<Badge color="teal">Board Certified</Badge>
<Badge color="gold">Accepting New Patients</Badge>
```

Colors: `blue` · `teal` (default) · `gold` · `navy` · `green`. Keep the label to 1–3 words; text renders at 12px/600.
